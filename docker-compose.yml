version: '3.8'
services:
  api:
    image: node:20  
    working_dir: /app
    volumes:
      - ./nestjs-cinema:/app 
    command: >
      bash -c "npm install && npm run start"
    ports:
      - "3000:3000"
